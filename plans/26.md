# Plan 26 — Skills lifecycle command parity

## Context

- [x] Read `/home/ubuntu/code/workspace/skills/CLAUDE.md`.
- [x] Reviewed the current `create-{ts,py,clj}-bigconfig-package` skill templates and checklists.
- [x] Checked the package spec: packages should expose lifecycle commands under `package <step>...`.

## User decisions

- Remove every top-level validation shortcut (`<pkg> validate`, `bb run validate`, and the TS `npm run validate` script); `package validate` is the only validation command.
- `package create` should be safe and render-only, using the same `hello` render workflow as `build`.
- `package delete` should be an explicit no-op workflow that exits successfully.
- Add a simple `package describe` command.
- Verification stays as currently scoped: direct generated-package commands, not extra `bc-pkg` local-path launcher checks.

## Working assumptions

- `package describe` is pure/no-side-effect and prints a concise static/profile summary, then exits 0.
- Verify with a hyphenated sample package per language so identifier/path token derivations are exercised.

## Implementation checklist

- [x] Update all three skill procedures (`SKILL.md`) so they say generated packages expose `validate`, `describe`, `build`, `create`, and `delete` under `package <step>...` and no longer advertise a top-level `validate` shortcut.
- [x] Update all three CLI templates:
  - help text shows `package validate|describe|build|create|delete` examples;
  - remove the top-level `validate` command branch;
  - update missing-step usage strings.
- [x] Update package workflow templates:
  - TypeScript/Python: import/use `WF_CREATE_FN`, `WF_DELETE_FN`, `WF_DESCRIBE_FN` / `CREATE_FN`, `DELETE_FN`, `DESCRIBE_FN`;
  - Clojure: merge `::workflow/create-fn`, `::workflow/delete-fn`, and `::workflow/describe-fn`;
  - define `create`, `delete`/`deleteWorkflow`, and `describe` consistently;
  - keep `build` rendering the `hello` stage;
  - make `create` render the same safe `hello` stage;
  - make `delete` a no-op success workflow;
  - make `describe` simple, pure/no-side-effect, and successful.
- [x] Update docs/checklists/templates:
  - generated READMEs list `package validate`, `package describe`, `package build`, `package create`, `package delete`;
  - checklists require the five lifecycle commands;
  - remove the TS `package.json` `validate` script.
- [x] Update generated tests:
  - direct lifecycle smoke test for `package validate`, `package describe`, `package build`, `package create`, `package delete`;
  - keep the `BC_PAR_NAME=REPLACE_ME package validate` failure check;
  - assert `build` and `create` render `greeting.txt` containing `Hello, world!`.
- [x] Verify by instantiating temporary hyphenated packages from each skill (`data-box`) and running:
  - TS: `npm install`, `npm run build`, `npm run typecheck`, `npm test`, `node run package validate|describe|build|create|delete`;
  - Python: `uv sync`, `uv run pytest -q`, `uv run python run package validate|describe|build|create|delete`;
  - Clojure: `clojure -M:test`, `bb run package validate|describe|build|create|delete`.

## Verification notes

- Used local SDK path dependencies in `/tmp/skills-check/*-data-box` to avoid changing published pins.
- TS verification initially needed `npm install` in `big-config/typescript` so the local SDK's `selmer` dependency was present; this left no tracked changes, and the temporary `node_modules` was removed afterward.

