# 18 — Refresh all READMEs and sibling CLAUDE.md files

Status: implemented in this checkout (docs-only, no commits).

## Request

Update the README of every leaf (plus the workspace root) and the `CLAUDE.md`
sibling next to each one, so docs are accurate, consistent, and reflect the
current state of the workspace.

## Goal / intent

Full pass (all of):

- Make every README/CLAUDE accurate and fix stale content.
- Reflect recent changes: root `run` git command groups (plan 16), `bc-pkg`
  local-repo support (plan 17), and Walter now implemented in all three
  languages.
- Bring stub READMEs up to parity with their **siblings in the same library
  group** (e.g. `once/python` should be as informative as `once/clojure` /
  `once/typescript`), without forcing one global template.
- Document the specific current behavior of each package.

## Files in scope

Each row = update `README.md` **and** the co-located `CLAUDE.md`.

- `./` (root `README.md` + root `CLAUDE.md`)
- `./selmer/python`
- `./selmer/typescript`
- `./big-config/clojure`
- `./big-config/python`
- `./big-config/typescript`
- `./once/clojure`
- `./once/python`
- `./once/typescript`
- `./walter/clojure`
- `./walter/python`
- `./walter/typescript`
- `./launcher/python`
- `./launcher/typescript`

That is 14 `README.md` + 14 `CLAUDE.md` files (root counts as one entry for
both).

**Out of scope:** the entire `selmer/clojure/` directory is ignored — do not
touch its `README.md` (upstream Selmer doc, ~42 KB) or its `CLAUDE.md`.

## Conventions to apply

- **Do NOT impose a single shared skeleton across all leaves.** Keep each
  leaf's natural structure; use the fuller siblings as reference for what a
  stub should cover (`once/clojure`, `walter/clojure` are good models).
- **Keep README vs CLAUDE separation:**
  - README = user-facing: purpose, role in the `selmer → big-config →
    once|walter` chain, install/sync, test, build, runnable CLI examples,
    conventions, license.
  - CLAUDE = agent/dev-facing: project overview, commands, architecture/module
    map, parity notes, working branch, "do not commit unless asked".
- Keep kebab-case parameter keys and namespaced reserved keys consistent in
  examples across languages.
- No legacy `amiorin` namespace/prefix references anywhere.

## Known issues to fix (non-exhaustive)

- `once/python/README.md` is a 220 B stub — expand to match its siblings.
- `walter/python` and `walter/typescript` READMEs are ~870 B stubs — expand to
  match `walter/clojure`.
- `big-config/typescript/README.md` references a local Selmer package at
  `../../Selmer/typescript` (wrong case; root README says TS pins Selmer via a
  GitHub commit) — correct it.
- Ensure root README/CLAUDE describe the grouped root `run` CLI
  (`bb run git report` / `bb run git commit`) and `bc-pkg` local-repo support.

## Verification

- No need to run commands. Verify by inspection.
- Every shell command shown is correct for that leaf (entry-point names,
  scripts, branch).
- Cross-language parity statements match reality (six-stage Once pipeline,
  Walter stages, shared `.dist/` artifacts).
- Internal relative paths and links resolve; fix stale ones.

## Git

Leaves are independent repos on their working branch (`main` / `clojure` /
`python` / `typescript`). **Do not commit** — docs-only edits, no commits.

## Changes made

Fixed (stale/incorrect facts):

- `once/clojure/README.md` + `once/clojure/CLAUDE.md` — big-config intro link
  `amiorin` → `bigconfig-ai` (the `deps.edn` coordinate quote is left verbatim,
  it is accurate).
- `once/typescript/README.md` — "shared local `big-config` (`../../big-config/typescript`)"
  → GitHub-pinned with optional `file:` override.
- `big-config/typescript/README.md` — wrong-case local `../../Selmer/typescript`
  → GitHub-pinned `bigconfig-ai/Selmer` with optional `file:` override.
- `big-config/python/README.md` — dropped the misleading "Clojure implementation
  removed" framing (siblings exist), removed the stale hard-coded Selmer SHA and
  the brittle `31 passed` count.
- `selmer/typescript/README.md` — install note reflects the GitHub-pinned usage
  instead of `npm install selmer`.
- Root `CLAUDE.md` — "eleven leaves" → fourteen; added Walter to the tree and the
  dependency-direction block; documented the grouped root `run` git CLI and the
  `bc-pkg` local-path support; added a Walter package-concepts section.
- Root `README.md` — added a Workspace tooling section (`bb run git ...`) and a
  `bc-pkg` local-path example.

Expanded (stubs brought to parity with their siblings):

- `once/python/README.md` (was a 220 B stub).
- `walter/python/README.md` and `walter/typescript/README.md` (were ~870 B stubs).

Left as-is (already accurate / out of scope):

- `selmer/clojure/*` — ignored per decision.
- `selmer/python/*`, `selmer/typescript/CLAUDE.md`, `big-config/clojure/*`,
  `big-config/typescript/CLAUDE.md`, `big-config/python/CLAUDE.md`,
  `once/typescript/CLAUDE.md`, `once/python/CLAUDE.md`, `walter/clojure/*`,
  `walter/python/CLAUDE.md`, `walter/typescript/CLAUDE.md`,
  `launcher/{python,typescript}/{README,CLAUDE}.md` — verified accurate.

Note: vendored `node_modules/big-config/README.md` copies under
`once/typescript` and `walter/typescript` still show the old Selmer path; those
are installed dependencies (not source) and refresh on the next dep bump.
