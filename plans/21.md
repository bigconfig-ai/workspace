<<<<<<< HEAD
# 21 — Add `once-ai` to `bb run git setup`

Status: implemented in this checkout (root `run` + root docs + root `.gitignore`).

## Original request

> execute plan `/Users/amiorin/code/bigconfig/plans/20.md` for
> `/Users/amiorin/code/bigconfig/once-ai/main`

## Review summary

`plans/20.md` added standalone primary-clone repositories to the **root**
workspace bootstrap manifest used by `bb run git setup`.

Interpreting this request the same way means: add the existing
`once-ai/main` repository to the root workspace setup, not edit the deployment
logic inside `once-ai/main`.

## Current state reviewed

- Root `run` has a `workspace-projects` manifest and already supports projects
  with no linked worktrees via `:worktrees []`.
- Before implementation, setup topology was documented as **seven primary clones
  plus nine linked worktrees**.
- `once-ai/main` exists in this checkout as its own git repository.
- `once-ai/main/CLAUDE.md` says this is a concrete BigConfig `once` deployment
  package instance for the live `bigconfig.ai` infrastructure, not a library
  leaf. Stay on `main`; do not commit unless asked.
- The `once-ai/main` package source is mostly its `run` file plus one local
  plugin. For this task, no changes inside `once-ai/main` appear necessary.
- Before implementation, root `.gitignore` ignored `/once-bigconfig/` and
  `/once-bigconfig-marketplace/`, but did **not** ignore `/once-ai/`; root git
  status therefore showed `?? once-ai/`.

Observed repository metadata:

| Relative path | Observed origin | Observed branch | Observed topology |
|---|---|---|---|
| `once-ai/main` | `git@github.com:bigconfig-ai/once-ai.git` | `main` | primary clone (`.git` directory) |

## Proposed behavior

Treat `once-ai` as a standalone primary-clone project with no linked worktrees:

| Project | Primary path | Remote | Primary branch | Linked worktrees |
|---|---|---|---|---|
| `once-ai` | `once-ai/main` | `git@github.com:bigconfig-ai/once-ai.git` | `main` | none |

Expected `setup` behavior should match the standalone repos added in plan 20:

- If the path is missing, run
  `git clone --branch main git@github.com:bigconfig-ai/once-ai.git <root>/once-ai/main`.
- If the path exists, validate it is a primary clone (`.git` directory), has the
  expected `origin`, and is on `main`.
- Run `git fetch --prune origin`.
- Refuse to pull if `git status --porcelain` is non-empty.
- Run `git pull --ff-only origin main`.
- Do not create linked worktrees.

## Decisions from user

1. Add `once-ai/main` to the **root** `bb run git setup` manifest. Do not change
   deployment files inside `once-ai/main` for this task.
2. Group `once-ai` with the Once-related repositories in `workspace-projects`.
   Order: `once` → `once-bigconfig` → `once-bigconfig-marketplace` → `once-ai`
   → `selmer`.
3. Use the observed SSH remote with `.git` suffix as canonical:
   `git@github.com:bigconfig-ai/once-ai.git`.
4. Update root docs by changing the topology count from “seven primary clones
   plus nine linked worktrees” to “eight primary clones plus nine linked
   worktrees”. Do not add a broader explicit `once-ai` project description.
5. Add `/once-ai/` to root `.gitignore` so the nested repo is not reported as an
   untracked directory by the root repo.

## Implementation plan

1. Edit root `run` and add a standalone project entry, preferably after
   `once-bigconfig-marketplace` and before `selmer`:

   ```clojure
   {:name "once-ai"
    :remote "git@github.com:bigconfig-ai/once-ai.git"
    :primary {:rel "once-ai/main" :branch "main"}
    :worktrees []}
   ```

2. Keep setup validation behavior unchanged. Existing primary-clone logic covers
   clone, validate, fetch, clean-check, and pull.
3. Update root `.gitignore`:
   - add `/once-ai/`.
4. Update root documentation counts:
   - `CLAUDE.md`: change setup topology from seven primary clones to eight.
   - `README.md`: change setup topology from seven primary clones to eight.
5. Do not edit `once-ai/main` source, generated `.dist/`, or secrets.

## Changes made

- Added `once-ai` to root `run` `workspace-projects` as a standalone primary
  clone at `once-ai/main` on `main`.
- Placed `once-ai` immediately after `once-bigconfig-marketplace` and before
  `selmer`.
- Added `/once-ai/` to root `.gitignore`.
- Updated root `README.md` and `CLAUDE.md` topology counts from seven primary
  clones to eight primary clones, keeping nine linked worktrees.
- Did not edit `once-ai/main` deployment source, generated artifacts, or
  secrets.

## Verification run

- `bb run` — parsed and printed top-level help successfully.
- `bb run git` — parsed and printed git help successfully.
- `bb run git setup --dry-run --root-dir <fresh-temp-dir>` — exited zero,
  included clone/fetch/pull dry-run commands for `once-ai/main`, and left the
  temp directory empty.
- `bb run git setup --dry-run` from this checkout validated `once-ai/main` and
  printed its fetch dry-run command, then exited non-zero because existing
  listed repos have unrelated uncommitted changes (`once-bigconfig/main` and,
  currently, `once-ai/main`). This confirms the existing clean-worktree guard
  still applies.
- `bb run git report --porcelain` — exited zero and included `once-ai/main`
  when present.
- `git status --short --branch` — no longer reports `?? once-ai/` after the
  `.gitignore` update.

## Verification plan for a clean/networked run

- `bb run git setup --root-dir <fresh-temp-dir>` creates `once-ai/main` as a
  primary clone on `main` with the expected SSH origin.
- `git -C <fresh-temp-dir>/once-ai/main branch --show-current` prints `main`.
- Re-running setup validates and fast-forwards `once-ai/main` without creating
  any worktrees.

## Out of scope

- Do not run live deployment commands from `once-ai/main`.
- Do not edit `.envrc`, `.dist/`, Terraform/OpenTofu state, or generated files.
- Do not commit or push unless explicitly asked.
