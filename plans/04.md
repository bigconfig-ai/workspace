- ignore the files in the current directory (/home/ubuntu/code/bigconfig/*)
- modify only the projects:
once/python/
once/typescript/
once/clojure/

so that the following CLIs are valid in the root of the project:
- clojure
bb run once package validate
- python
uv run once package validate
uv run once tofu render
uv run once tofu-dns render
- typescript
npm run once package validate

# Implementation Notes
- the clojure solution needs to use a babashka script named run in the root of the project
