# 20 — Add `once-bigconfig` repos to `bb run git setup`

Status: implemented in this checkout (root `run` + root docs).

## Request

Add these repositories to the root workspace bootstrap command:

- `/Users/amiorin/code/bigconfig/once-bigconfig/main`
- `/Users/amiorin/code/bigconfig/once-bigconfig-marketplace/main`

The implementation should use paths relative to `--root-dir`, not hard-coded
absolute paths:

- `once-bigconfig/main`
- `once-bigconfig-marketplace/main`

## Current state reviewed

The root `run` Babashka script already has `bb run git setup` backed by a
`workspace-projects` manifest. Before this change it bootstrapped five upstream
projects:

- `big-config` — primary clone plus language worktrees
- `launcher` / `bc-pkg` — primary clone plus language worktree
- `once` — primary clone plus language worktrees
- `selmer` — primary clone plus language worktrees
- `walter` — primary clone plus language worktrees

The two requested paths exist in this checkout as standalone primary clones:

| Relative path | Observed origin | Observed branch | Observed topology |
|---|---|---|---|
| `once-bigconfig/main` | `git@github.com:bigconfig-ai/once-bigconfig.git` | `main` | primary clone (`.git` directory) |
| `once-bigconfig-marketplace/main` | `git@github.com:bigconfig-ai/once-bigconfig-marketplace.git` | `main` | primary clone (`.git` directory) |

`run` already supports projects with no linked worktrees if the manifest uses
`:worktrees []`.

## Proposed behavior

Treat both requested repositories as standalone primary-clone projects with no
linked worktrees:

| Project | Primary path | Remote | Primary branch | Linked worktrees |
|---|---|---|---|---|
| `once-bigconfig` | `once-bigconfig/main` | `git@github.com:bigconfig-ai/once-bigconfig.git` | `main` | none |
| `once-bigconfig-marketplace` | `once-bigconfig-marketplace/main` | `git@github.com:bigconfig-ai/once-bigconfig-marketplace.git` | `main` | none |

Expected `setup` behavior for each new project should match existing primary
clone behavior:

- If the path is missing, run `git clone --branch main <remote> <path>`.
- If the path exists, validate it is a primary clone (`.git` directory), has the
  expected `origin`, and is on `main`.
- Run `git fetch --prune origin`.
- Refuse to pull if `git status --porcelain` is non-empty.
- Run `git pull --ff-only origin main`.
- Do not create linked worktrees for these repos.

## Decisions from user

1. Treat both repos as **standalone primary clones with no linked worktrees**.
2. Use the observed SSH remotes, including `.git` suffixes, as canonical:
   - `git@github.com:bigconfig-ai/once-bigconfig.git`
   - `git@github.com:bigconfig-ai/once-bigconfig-marketplace.git`
3. Use `main` as the intended branch for both repos.
4. Add them immediately after `once` in `workspace-projects`, before `selmer`,
   so Once-related repositories stay grouped.
5. Let `bb run git report` / `bb run git commit` include these repos whenever
   present. They already will via `.git` discovery; adding them to `setup` means
   fresh workspaces will also get them.
6. Update root docs to describe the workspace as **seven primary clones plus nine
   linked worktrees**.

## Implementation plan

1. Edit root `run` and add two entries to `workspace-projects`, preferably after
   the existing `once` entry:

   ```clojure
   {:name "once-bigconfig"
    :remote "git@github.com:bigconfig-ai/once-bigconfig.git"
    :primary {:rel "once-bigconfig/main" :branch "main"}
    :worktrees []}
   {:name "once-bigconfig-marketplace"
    :remote "git@github.com:bigconfig-ai/once-bigconfig-marketplace.git"
    :primary {:rel "once-bigconfig-marketplace/main" :branch "main"}
    :worktrees []}
   ```

2. Keep setup validation behavior unchanged. The existing code path for primary
   clones covers clone, validate, fetch, clean-check, and pull.
3. Update root documentation:
   - `CLAUDE.md` workspace tooling line: change “five primary clones” to
     “seven primary clones”.
   - `README.md`: mention that setup creates/validates seven primary clones plus
     nine linked worktrees.
4. Do not edit `.gitignore` unless asked; it already ignores
   `/once-bigconfig/` and `/once-bigconfig-marketplace/`.

## Changes made

- Added `once-bigconfig` to root `run` `workspace-projects` as a standalone
  primary clone at `once-bigconfig/main` on `main`.
- Added `once-bigconfig-marketplace` to root `run` `workspace-projects` as a
  standalone primary clone at `once-bigconfig-marketplace/main` on `main`.
- Placed both new projects immediately after `once` and before `selmer`.
- Updated root `CLAUDE.md` to describe setup as seven primary clones plus nine
  linked worktrees.
- Updated root `README.md` with the same seven-primary / nine-worktree topology.

## Verification run

- `bb run` — parsed and printed help successfully.
- `bb run git` — parsed and printed git help successfully.
- `bb run git setup --dry-run --root-dir <fresh-temp-dir>` — exited zero,
  included clone plans for:
  - `once-bigconfig/main`
  - `once-bigconfig-marketplace/main`
  and left the temp directory empty.
- `bb run git setup --dry-run` from this checkout validated both new primary
  clone entries and printed fetch/pull dry-run commands, then exited non-zero
  because the existing `once-bigconfig/main` checkout has unrelated uncommitted
  changes (`plans/20.md`). This confirms the existing clean-worktree guard still
  applies to the new repos.
- `bb run git report --porcelain` — exited zero and reported 17 repos total
  (root + 16 nested repos), including the two new repos when present.
- `bb run git commit --dry-run` — exited zero and included `once-bigconfig/main`
  in the dry-run dirty-repo list when dirty.

## Verification plan for a clean/networked run

- `bb run git setup --root-dir <fresh-temp-dir>` creates both repos as primary
  clones on `main` with the expected SSH origins.
- `git -C <fresh-temp-dir>/once-bigconfig/main branch --show-current` prints
  `main`.
- `git -C <fresh-temp-dir>/once-bigconfig-marketplace/main branch --show-current`
  prints `main`.
- Re-running setup validates and fast-forwards both repos without creating any
  worktrees.
